export interface UserType {
    name: string;
    age: string;
    accountId: string;
    createAt: number;
}

export interface AccountType {
    username: string;
    password: string;
    createAt: number;
}
